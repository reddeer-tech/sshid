dist/
.DS_Store

# never commit credentials — a publish token can push code to every machine
# that installs this package
.npmrc
.env
.env.*

# release tooling: kept on this machine, deliberately not published.
# these build and ship the package, they are not part of it.
Makefile
RELEASING.md
Formula/
ansible/
